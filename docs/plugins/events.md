# Events

Subscriptions are typed descriptors such as `ProjectOpened`, `MissionCompleted`, or `ArtifactCreated`. Phase 17 filters project and classification visibility. Persistent background handlers require a declared background permission and become bounded, cancellable Phase 4 work; there is no raw global event bus.

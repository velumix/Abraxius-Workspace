# Compute extensions

Model backend, model source, and telemetry contributions register behind Phase 21 boundaries. A backend plugin cannot bypass local admission, reservations, resource truth, sandboxing, or Phase 17. Vendor telemetry is treated as input that the governor validates rather than unquestioned authority.

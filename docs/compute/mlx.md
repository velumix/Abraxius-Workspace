# MLX

MLX/MLX-LM is represented as an Apple Silicon provider boundary. Apple devices use unified-memory accounting rather than fake dedicated VRAM. Streaming, quantization, and prompt caching are backend capabilities, never universal assumptions.

# Windows ML

The Windows provider boundary treats CPU, GPU, and NPU execution-provider devices as discoverable resources. Device lists and compiled-cache compatibility must be refreshed after runtime, provider, or driver changes. DXGI process budgets can constrain effective GPU memory.

The native Windows ML adapter remains an extension boundary on non-Windows builds.

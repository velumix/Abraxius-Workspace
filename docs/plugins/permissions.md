# Permissions

Permissions are declared before execution. Undeclared requests are denied. Required permissions must be reviewed before installation. Updates compare old and new declarations: additions or broadened scope require review, while reductions retain an audit trail. Secret settings store a `SecretRef`, never secret material.

# Testing

The plugin test project covers bounded static parsing, permission review, tamper identity, permission expansion, declarative UI rejection, namespaced storage, bounded logs, registration rejection, supervisor isolation, and Fabric advertisement. Add golden protobuf fixtures for every supported protocol generation and compiled previous-SDK fixture packages before declaring cross-version support stable.

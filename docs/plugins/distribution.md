# Distribution

Phase 22 supports local packages and configured NuGet-compatible source abstractions; it is not a public marketplace. Installed payloads are immutable, registry metadata selects an active side-by-side version, and installation location/trust/grants are explicit Phase 23 backup inputs. Private keys and raw secrets remain outside plugin backup data.

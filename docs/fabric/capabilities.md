# Capabilities and resources

Capabilities are structured IDs with versions and properties. Placement never infers tools from an operating system name. Resource snapshots include CPU headroom, RAM, GPU/VRAM, disk, power, thermal state, and bounded network observations. Model inventory is separate from Phase 6 routing policy.

# Capabilities

Plugin capabilities declare input/output schemas, side effects, risk, timeout, idempotency, and required permissions. Execution remains `Phase 4 -> Lattice -> Security -> plugin proxy -> PluginHost`. Plugin declarations are evidence for review, not trusted security classification.

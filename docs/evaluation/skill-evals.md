# Skill evaluation

Skill evaluation pairs the same fixture with fresh planning and an exact Skill version. Metrics include verified success, latency, model calls, tokens, cost, replans, evidence quality, and environment compatibility.

Eval evidence may support validation but cannot fabricate production reliability or procedural trust. A new Skill version is not promoted merely because it exists. Synthetic eval execution neither awards progression nor triggers automatic Skill extraction.

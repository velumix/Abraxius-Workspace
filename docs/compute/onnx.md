# ONNX

ONNX packages are immutable variants. Filename extensions are not trusted, and imported data remains unvalidated until a backend parses and load-tests it. ONNX Runtime GenAI remains behind an experimental boundary so preview API shapes do not leak into the compute domain.

# Fabric

Phase 20 advertises compact installed-plugin capability records containing exact plugin and contribution versions. Placement remains eligible only where the exact plugin is installed, trusted, enabled, platform-compatible, and authorized. Packages are not transferred per invocation. Remote results preserve node, plugin, version, and contribution provenance.

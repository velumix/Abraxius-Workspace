# Fabric UI

The Avalonia Fabric surface reads actual runtime descriptors. Its virtualized node list shows health, trust, connectivity, resources, and capabilities; the inspector exposes stable identity and control actions. Compiled bindings catch contract errors at build time.

Telemetry refreshes are snapshot-based. The UI does not fabricate graphs, percentages, resource waves, or trust state.

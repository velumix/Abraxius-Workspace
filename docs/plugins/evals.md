# Evaluation

Plugin-provided suites run under plugin/project policy and pin plugin version. Built-in Phase 19 coverage should evaluate protocol compatibility, lifecycle, permissions, sandbox claims, malformed previews, UI update pressure, crash recovery, IPC overhead, and built-in versus out-of-process capability cost. Evaluation work remains progression-ineligible.

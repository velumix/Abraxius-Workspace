# ExamplePlugin

Build the plugin, package its published payload with `abraxius.plugin.json`, validate the package with `abraxius plugins validate`, and install it only in Developer Mode until it is signed by an approved source.
